diff --git a/build/universal/Slider/types.d.ts b/build/universal/Slider/types.d.ts
index 98749050812c63417c046f8c1ab9b68c0518901d..5a554398df7ba18afc4a561e166f611ec74bbec3 100644
--- a/build/universal/Slider/types.d.ts
+++ b/build/universal/Slider/types.d.ts
@@ -1,3 +1,4 @@
+import type { ColorValue } from 'react-native';
 import type { ModifierConfig } from '../../types';
 /**
  * Props for the [`Slider`](#slider) component.
@@ -10,7 +11,11 @@ export interface SliderProps {
     /**
      * Called when the user changes the slider value.
      */
-    onValueChange: (value: number) => void;
+    onValueChange?: (value: number) => void;
+    /**
+     * Called when the user finishes changing the slider value.
+     */
+    onSlidingComplete?: (value: number) => void;
     /**
      * Minimum value of the slider range.
      * @default 0
@@ -38,5 +43,19 @@ export interface SliderProps {
      * from `@expo/ui/swift-ui/modifiers` or `@expo/ui/jetpack-compose/modifiers`.
      */
     modifiers?: ModifierConfig[];
+    /**
+     * Color of the track before the thumb.
+     */
+    minimumTrackTintColor?: ColorValue;
+    /**
+     * Color of the track after the thumb.
+     * @platform android
+     */
+    maximumTrackTintColor?: ColorValue;
+    /**
+     * Color of the thumb.
+     * @platform android
+     */
+    thumbTintColor?: ColorValue;
 }
 //# sourceMappingURL=types.d.ts.map
\ No newline at end of file
diff --git a/src/universal/Slider/index.android.tsx b/src/universal/Slider/index.android.tsx
index f12eb5d88b358d55307536a82fab6b0c14b6c29d..900bab43c0a4b54425b21394b1e2f056d32d6c0f 100644
--- a/src/universal/Slider/index.android.tsx
+++ b/src/universal/Slider/index.android.tsx
@@ -1,3 +1,4 @@
+import { useRef } from 'react';
 import { Slider as ComposeSlider } from '@expo/ui/jetpack-compose';
 import { testID as testIDModifier } from '@expo/ui/jetpack-compose/modifiers';
 
@@ -6,13 +7,18 @@ import type { SliderProps } from './types';
 export function Slider({
   value,
   onValueChange,
+  onSlidingComplete,
   min = 0,
   max = 1,
   step,
   disabled,
   testID,
   modifiers,
+  minimumTrackTintColor,
+  maximumTrackTintColor,
+  thumbTintColor,
 }: SliderProps) {
+  const lastValueRef = useRef(value);
   // Android `steps` = number of discrete intervals between min and max.
   // Universal `step` = increment size. Convert: steps = (max - min) / step - 1.
   let steps: number | undefined;
@@ -23,9 +29,12 @@ export function Slider({
 
   const handleValueChange = disabled
     ? undefined
-    : step != null && step > 0
-      ? (v: number) => onValueChange(Math.round((v - min) / step) * step + min)
-      : onValueChange;
+    : (v: number) => {
+        const nextValue = step != null && step > 0 ? Math.round((v - min) / step) * step + min : v;
+
+        lastValueRef.current = nextValue;
+        onValueChange?.(nextValue);
+      };
 
   return (
     <ComposeSlider
@@ -36,6 +45,14 @@ export function Slider({
       steps={steps}
       enabled={!disabled}
       modifiers={[...(modifiers ?? []), ...(testID ? [testIDModifier(testID)] : [])]}
+      colors={{
+        activeTrackColor: minimumTrackTintColor,
+        inactiveTrackColor: maximumTrackTintColor,
+        thumbColor: thumbTintColor,
+      }}
+      onValueChangeFinished={
+        onSlidingComplete === undefined ? undefined : () => onSlidingComplete(lastValueRef.current)
+      }
     />
   );
 }
diff --git a/src/universal/Slider/index.ios.tsx b/src/universal/Slider/index.ios.tsx
index e491b4849d630d7bc2235dd181fb3642786d55fd..86bf1afc78fece65f7fbe58618e4ff9d51046134 100644
--- a/src/universal/Slider/index.ios.tsx
+++ b/src/universal/Slider/index.ios.tsx
@@ -1,5 +1,6 @@
+import { useRef } from 'react';
 import { Slider as SwiftUISlider } from '@expo/ui/swift-ui';
-import { disabled as disabledMod } from '@expo/ui/swift-ui/modifiers';
+import { disabled as disabledMod, tint as tintMod } from '@expo/ui/swift-ui/modifiers';
 import type { ModifierConfig } from '@expo/ui/swift-ui/modifiers';
 
 import type { SliderProps } from './types';
@@ -7,21 +8,35 @@ import type { SliderProps } from './types';
 export function Slider({
   value,
   onValueChange,
+  onSlidingComplete,
   min = 0,
   max = 1,
   step,
   disabled,
   testID,
   modifiers: extraModifiers,
+  minimumTrackTintColor,
 }: SliderProps) {
+  const lastValueRef = useRef(value);
   const modifiers: ModifierConfig[] = [];
   if (disabled) modifiers.push(disabledMod(true));
+  if (minimumTrackTintColor !== undefined) modifiers.push(tintMod(minimumTrackTintColor as string));
   if (extraModifiers) modifiers.push(...extraModifiers);
 
+  const handleValueChange = (nextValue: number) => {
+    lastValueRef.current = nextValue;
+    onValueChange?.(nextValue);
+  };
+
   return (
     <SwiftUISlider
       value={value}
-      onValueChange={onValueChange}
+      onValueChange={handleValueChange}
+      onEditingChanged={(isEditing) => {
+        if (!isEditing) {
+          onSlidingComplete?.(lastValueRef.current);
+        }
+      }}
       min={min}
       max={max}
       step={step}
diff --git a/src/universal/Slider/index.tsx b/src/universal/Slider/index.tsx
index b34f4aa9bb2bd28ed18b55d69eba948dfe322d2d..08a64ad548f22a39d12eabe316699996e6f63035 100644
--- a/src/universal/Slider/index.tsx
+++ b/src/universal/Slider/index.tsx
@@ -1,3 +1,4 @@
+import { useRef } from 'react';
 import { StyleSheet, View, type ViewStyle } from 'react-native';
 
 import { colors, createWebComponent, css, durations, easings, shadows } from '../webUtils';
@@ -97,13 +98,23 @@ const sliderCss = css`
 export function Slider({
   value,
   onValueChange,
+  onSlidingComplete,
   min = 0,
   max = 1,
   step,
   disabled = false,
   testID,
+  minimumTrackTintColor,
 }: SliderProps) {
+  const lastValueRef = useRef(value);
   const pct = ((value - min) / (max - min)) * 100;
+  const handleValueChange = (nextValue: number) => {
+    lastValueRef.current = nextValue;
+    onValueChange?.(nextValue);
+  };
+  const handleSlidingComplete = () => {
+    onSlidingComplete?.(lastValueRef.current);
+  };
-
+
   return (
     <View style={styles.view}>
@@ -119,9 +130,20 @@ export function Slider({
         max={max}
         step={step ?? 'any'}
         disabled={disabled}
-        onChange={({ target: { value } }) => onValueChange(Number.parseFloat(value))}
+        onChange={({ target: { value } }) => handleValueChange(Number.parseFloat(value))}
+        onPointerUp={handleSlidingComplete}
+        onTouchEnd={handleSlidingComplete}
         data-testid={testID}
-        style={[styles.input, { '--_pct': `${pct}%` } as ViewStyle, disabled && styles.disabled]}
+        style={[
+          styles.input,
+          {
+            '--_pct': `${pct}%`,
+            ...(minimumTrackTintColor !== undefined && {
+              '--_track-fill': minimumTrackTintColor as string,
+            }),
+          } as ViewStyle,
+          disabled && styles.disabled,
+        ]}
       />
     </View>
   );
diff --git a/src/universal/Slider/types.ts b/src/universal/Slider/types.ts
index 8c425c8b32821ad0b026f7dbfec213930fc8c57f..a0e76690fa00dab90bf3c044514fc62fe4fefb50 100644
--- a/src/universal/Slider/types.ts
+++ b/src/universal/Slider/types.ts
@@ -1,3 +1,4 @@
+import type { ColorValue } from 'react-native';
 import type { ModifierConfig } from '../../types';
 
 /**
@@ -12,7 +13,12 @@ export interface SliderProps {
   /**
    * Called when the user changes the slider value.
    */
-  onValueChange: (value: number) => void;
+  onValueChange?: (value: number) => void;
+
+  /**
+   * Called when the user finishes changing the slider value.
+   */
+  onSlidingComplete?: (value: number) => void;
 
   /**
    * Minimum value of the slider range.
@@ -46,4 +52,21 @@ export interface SliderProps {
    * from `@expo/ui/swift-ui/modifiers` or `@expo/ui/jetpack-compose/modifiers`.
    */
   modifiers?: ModifierConfig[];
+
+  /**
+   * Color of the track before the thumb.
+   */
+  minimumTrackTintColor?: ColorValue;
+
+  /**
+   * Color of the track after the thumb.
+   * @platform android
+   */
+  maximumTrackTintColor?: ColorValue;
+
+  /**
+   * Color of the thumb.
+   * @platform android
+   */
+  thumbTintColor?: ColorValue;
 }
